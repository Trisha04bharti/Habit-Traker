# 📱 Habit Tracker App

A multi-user Habit Tracker built using **SwiftUI** with `@State` and `@Binding`.

This project focuses on understanding deep SwiftUI data flow using nested bindings and reactive UI updates — without using `ObservableObject` or MVVM.

---

## 🚀 Features

- Add & delete users
- Add, edit & delete habits
- Set habit targets
- Track daily progress
- Toggle habit completion
- Per-user completion stats
- Overall app progress tracking

---

## 🧠 Concepts Practiced

- `@State` as source of truth
- `@Binding` for child-to-parent updates
- Binding inside `ForEach`
- Nested data structures (Array inside Array)
- Derived state using computed properties
- Reactive UI updates

---

## 🛠 Built With

- Swift
- SwiftUI
- NavigationView
- List & ForEach
- ProgressView

---

## 🎯 Goal

To deeply understand how SwiftUI state and bindings work in complex, multi-level view hierarchies


